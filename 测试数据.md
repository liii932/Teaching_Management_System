# 教务系统测试数据格式

本文档根据 `src/main/resources/db/edusystem.sql`、模型类和 `TeachingService` 校验规则整理。各表存在外键依赖，建议按以下顺序准备或导入测试数据：

1. 系信息 `department`
2. 专业信息 `major`
3. 班级信息 `class`
4. 学生信息 `student`
5. 教师信息 `teacher`
6. 课程信息 `course`
7. 成绩信息 `sc`

## 通用规则

- 字符集：`utf8mb4`
- 性别字段：只能填写 `男` 或 `女`；不填时应用层默认使用 `男`
- 年龄：可为空；填写时范围为 `0` 到 `120`
- 学分：可为空；不填时应用层默认 `0.0`；填写时不能为负数
- 成绩：可为空；填写时范围为 `0` 到 `100`
- 编号字段有固定长度要求，长度不符会被应用层拒绝
- 外键字段如果不为空，必须引用已存在的数据

## 系信息 department

| 字段 | 含义 | 数据格式 | 是否必填 | 约束/说明 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `DeptNo` | 系编号 | `char(4)`，4 位字符串 | 是 | 主键，不能重复 | `CS01` |
| `DeptName` | 系名称 | `varchar(30)` | 是 | 不能为空 | `计算机系` |

示例：

```sql
INSERT INTO department (DeptNo, DeptName)
VALUES ('CS01', '计算机系');
```

## 专业信息 major

| 字段 | 含义 | 数据格式 | 是否必填 | 约束/说明 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `MajorNo` | 专业编号 | `char(6)`，6 位字符串 | 是 | 主键，不能重复 | `CS0101` |
| `MajorName` | 专业名称 | `varchar(30)` | 是 | 不能为空 | `软件工程` |
| `DeptNo` | 所属系编号 | `char(4)`，4 位字符串 | 是 | 外键，必须存在于 `department.DeptNo` | `CS01` |

示例：

```sql
INSERT INTO major (MajorNo, MajorName, DeptNo)
VALUES ('CS0101', '软件工程', 'CS01');
```

## 班级信息 class

| 字段 | 含义 | 数据格式 | 是否必填 | 约束/说明 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `ClassNo` | 班级号 | `char(8)`，8 位字符串 | 是 | 主键，不能重复 | `CS230101` |
| `ClassName` | 班级名称 | `varchar(30)` | 否 | 可为空 | `软件2301班` |
| `MajorNo` | 所属专业编号 | `char(6)`，6 位字符串 | 是 | 外键，必须存在于 `major.MajorNo` | `CS0101` |

示例：

```sql
INSERT INTO class (ClassNo, ClassName, MajorNo)
VALUES ('CS230101', '软件2301班', 'CS0101');
```

## 学生信息 student

| 字段 | 含义 | 数据格式 | 是否必填 | 约束/说明 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `Sno` | 学号 | `char(10)`，10 位字符串 | 是 | 主键，不能重复 | `2023000001` |
| `Sname` | 学生姓名 | `varchar(20)` | 是 | 不能为空 | `张三` |
| `Ssex` | 性别 | `char(2)` | 否 | 只能为 `男` 或 `女`；默认 `男` | `男` |
| `Sage` | 年龄 | `smallint` | 否 | 范围 `0` 到 `120` | `20` |
| `ClassNo` | 所属班级号 | `char(8)`，8 位字符串 | 否 | 外键；为空表示暂未分班 | `CS230101` |

示例：

```sql
INSERT INTO student (Sno, Sname, Ssex, Sage, ClassNo)
VALUES ('2023000001', '张三', '男', 20, 'CS230101');
```

## 教师信息 teacher

| 字段 | 含义 | 数据格式 | 是否必填 | 约束/说明 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `Tno` | 教职工号 | `char(8)`，8 位字符串 | 是 | 主键，不能重复 | `T2023001` |
| `Tname` | 教师姓名 | `varchar(20)` | 是 | 不能为空 | `李老师` |
| `Tsex` | 性别 | `char(2)` | 否 | 只能为 `男` 或 `女`；默认 `男` | `女` |
| `Ttitle` | 职称 | `varchar(10)` | 否 | 可为空 | `副教授` |
| `ClassNo` | 负责班级号 | `char(8)`，8 位字符串 | 否 | 外键；为空表示不负责班级 | `CS230101` |

示例：

```sql
INSERT INTO teacher (Tno, Tname, Tsex, Ttitle, ClassNo)
VALUES ('T2023001', '李老师', '女', '副教授', 'CS230101');
```

## 课程信息 course

| 字段 | 含义 | 数据格式 | 是否必填 | 约束/说明 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `Cno` | 课程号 | `char(6)`，6 位字符串 | 是 | 主键，不能重复 | `C10001` |
| `Cname` | 课程名称 | `varchar(30)` | 是 | 不能为空 | `数据库原理` |
| `Credit` | 学分 | `decimal(3,1)` | 否 | 不能为负数；默认 `0.0` | `3.5` |
| `Tno` | 授课教师号 | `char(8)`，8 位字符串 | 否 | 外键；为空表示暂未安排教师 | `T2023001` |

示例：

```sql
INSERT INTO course (Cno, Cname, Credit, Tno)
VALUES ('C10001', '数据库原理', 3.5, 'T2023001');
```

## 成绩信息 sc

| 字段 | 含义 | 数据格式 | 是否必填 | 约束/说明 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `Sno` | 学号 | `char(10)`，10 位字符串 | 是 | 联合主键之一；必须存在于 `student.Sno` | `2023000001` |
| `Cno` | 课程号 | `char(6)`，6 位字符串 | 是 | 联合主键之一；必须存在于 `course.Cno` | `C10001` |
| `Grade` | 成绩 | `decimal(4,1)` | 否 | 可为空；填写时范围 `0` 到 `100` | `88.5` |

示例：

```sql
INSERT INTO sc (Sno, Cno, Grade)
VALUES ('2023000001', 'C10001', 88.5);
```
## 一组完整可用的测试数据

以下数据互相匹配，可按顺序直接插入空数据库：

```sql
INSERT INTO department (DeptNo, DeptName)
VALUES ('CS01', '计算机系');

INSERT INTO major (MajorNo, MajorName, DeptNo)
VALUES ('CS0101', '软件工程', 'CS01');

INSERT INTO class (ClassNo, ClassName, MajorNo)
VALUES ('CS230101', '软件2301班', 'CS0101');

INSERT INTO student (Sno, Sname, Ssex, Sage, ClassNo)
VALUES ('2023000001', '张三', '男', 20, 'CS230101');

INSERT INTO teacher (Tno, Tname, Tsex, Ttitle, ClassNo)
VALUES ('T2023001', '李老师', '女', '副教授', 'CS230101');

INSERT INTO course (Cno, Cname, Credit, Tno)
VALUES ('C10001', '数据库原理', 3.5, 'T2023001');

INSERT INTO sc (Sno, Cno, Grade)
VALUES ('2023000001', 'C10001', 88.5);
```
